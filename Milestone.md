明白了：**AlphaTrade 的唯一大目标**就是——

运行环境: conda 的alphatrade

> 借鉴 AlphaGenome 的工程化思想（稳定训练、强表达、可扩展 trunk），但做成**严格因果**的期货多周期预测器：输入 1min 因果特征窗口 → 输出多个 horizon 的“未来涨跌/收益分布（分位数）”，并且能用统一评估框架持续迭代。

下面我把 **北极星目标 → 里程碑**按这个目标重新“对齐整理”，让每个 milestone 都是为“多周期涨跌预测”服务的。

---

## 北极星目标拆成 3 条可验收的硬目标

1. **预测能力（核心）**

    * 对每个 horizon（如 1/5/20/60min）：能预测未来收益分布，并能导出“涨跌方向”。
2. **可比较（工程核心）**

    * 同一数据契约、同一模型入口、同一评估口径：多 seed/多配置/多 universe **可重复对比**。
3. **可扩展（产品化核心）**

    * universe 扩大、训练更久、指标更全，不会把 pipeline 搞崩；每轮迭代都能产出机器可读报告。

---

## 里程碑重新规划（每个都有硬性目标）

> 你们已经把 M0–M4 基本跑通了，我这里给的是“最终正确的里程碑语义”，避免以后再出现“做了但不算这个 milestone”的混乱。

### M0：数据最小闭环（证明“能学”）

* **硬目标**：从 archive → canonical → index → smoke train 跑通（模型可以很简单）
* **为什么对齐目标**：证明“用 1min 因果特征预测未来”这条链路成立

### M1：Universe 与数据契约规模化（证明“能扩”）

* **硬目标**：候选品种/主力可用性扫描 + 多品种 bars/index 生成稳定；**特征契约固定（8维）**
* **为什么对齐目标**：期货里非主力没流动性，必须把 universe 建在“可交易/可学习”的数据上

### M2：训练/报告契约固化（证明“能比较的前提”）

* **硬目标**：reports JSON 字段名固定 + schema + validator；训练输出可机器消费
* **为什么对齐目标**：没有固定 reports，就无法做多轮迭代的自动对比

### M3：AlphaTrade v0.2 真接入训练（证明“模型本体有效”）

* **硬目标**：训练脚本真正调用 AlphaTrade（JAX），稳定跑，输出固定 metrics + run.md
* **为什么对齐目标**：从“MLP占位”切到“AlphaGenome 思想的 trunk”

### M4：评估与可复现实验矩阵（证明“比较框架成立”）

* **硬目标**：

    * 多 seed 训练稳定性可复现
    * eval pipeline（含 fast）可复现
    * matrix_summary 一页看懂对比
    * schema_validation 全绿
* **为什么对齐目标**：你说得对：M4 的意义就是“评估流程与对比框架成立”，否则后面都是瞎迭代

---

## 接下来该怎么定义 M5（让迭代真正服务“多周期涨跌”）

> M5 不是“再跑一次”，而是第一次让评估指标**直接指导模型改动**。

### M5：可解释改动的 ablation（证明“能改进”）

* **硬目标**：固定 baseline（同 universe/同 steps/同 seeds），做 2–3 个**可解释改动**，matrix 对比能稳定看到收益（或明确退化）
* **改动建议（典型 3 类，任选 2–3 个）**

    1. **目标更贴近“涨跌”**：在不破坏分位数输出的前提下，加方向辅助 loss（例如 sign / margin）或用 q50 的方向做一致性约束
    2. **多品种训练更公平**：按 symbol 平衡采样 / loss reweight（避免大品种支配梯度）
    3. **多 horizon 权重策略**：让短周期/长周期的 loss 权重可配置，并在 eval 上看“哪类 horizon 真提升”
* **验收输出**：`reports/m5_matrix_summary.md` + `reports/m5_ablation_table.json`（字段名固定）

