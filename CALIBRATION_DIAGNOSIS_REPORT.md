# AlphaTrade 校准问题完整诊断报告

**日期**: 2026-02-25
**问题**: 模型校准严重失败（coverage 偏差 0.1-0.3）
**状态**: ✅ 已诊断，提供修复方案

---

## 🔍 问题现象

### 用户报告的症状

```
h=1:  coverage_q90=0.990 (应该 0.90) → 预测区间太宽
h=5:  coverage_q90=0.956 (应该 0.90) → 预测区间太宽
h=20: coverage_q90=0.529 (应该 0.90) → 预测区间太窄
h=60: coverage_q90=0.689 (应该 0.90) → 预测区间太窄
```

**ECE**: 0.13-0.30（理想应该 < 0.05）
**Eval Loss**: 0.0014（看起来很好，但误导性的）

---

## 🎯 诊断结果

### 1. 数据质量检查 ✅

**结论**: 数据有微弱但真实的信号

| Horizon | 最强特征相关性 | 线性模型 R² | 线性模型 Corr |
|---------|--------------|------------|--------------|
| h=1     | 0.1551       | 0.033      | 0.1822       |
| h=5     | 0.1037       | 0.045      | 0.2111       |
| h=20    | 0.1005       | 0.022      | 0.1481       |
| h=60    | 0.1551       | 0.041      | 0.2032       |

**解读**:
- ✅ 特征与目标有相关性（|corr| = 0.10-0.16）
- ✅ 简单线性模型可以达到 R²=2-4%
- ✅ 这在金融数据中是合理的信号强度

### 2. 模型性能检查 ❌

**结论**: 深度模型完全没有学到信号

| Horizon | 模型预测 Corr | 预测 Std | 真实 Std | Std 比率 |
|---------|--------------|----------|----------|---------|
| h=1     | -0.024       | 0.000266 | 0.001178 | 0.23    |
| h=5     | -0.043       | 0.000798 | 0.002960 | 0.27    |
| h=20    | +0.001       | 0.001353 | 0.005283 | 0.26    |
| h=60    | +0.013       | 0.002497 | 0.010001 | 0.25    |

**解读**:
- ❌ 预测与真实值几乎无关（corr ≈ 0）
- ❌ 预测几乎是常数（std 只有真实值的 1/4）
- ❌ **比线性回归差 5-10 倍**

### 3. 校准失败原因 ✅

**结论**: 模型学到了"无条件分位数"，不是"条件分位数"

| Horizon | IQR_真实 | IQR_预测 | Scale 比率 | 问题 |
|---------|---------|---------|-----------|------|
| h=1     | 0.001253 | 0.003179 | **2.54**  | 区间太宽 |
| h=5     | 0.002557 | 0.003938 | **1.54**  | 区间太宽 |
| h=20    | 0.004700 | 0.004248 | 0.90      | 接近正确 |
| h=60    | 0.008705 | 0.006892 | **0.79**  | 区间太窄 |

**为什么 Pinball Loss 小但校准差？**

```python
# 模型学到的是数据集的边缘分布：
q10 = np.quantile(y_all, 0.10)  # 常数
q50 = np.quantile(y_all, 0.50)  # 常数
q90 = np.quantile(y_all, 0.90)  # 常数

# 这样 Pinball Loss 会很小（预测了合理的分位数）
# 但没有条件信息（不依赖输入特征）
# 所以 calibration 失败
```

### 4. 特征检查 ⚠️

**发现**: Feature 7 是常数（全是 1.0）

```
Feature 7: mean=1.0, std=0.0, min=1.0, max=1.0
归一化后: 全是 0.0
```

**影响**: 轻微（浪费了一个特征维度，但不影响其他特征）

---

## 🔴 根本原因

### 模型容量 vs 数据量 vs 信号强度不匹配

**当前配置**:
```json
{
  "d_model": 128,
  "num_layers": 3,
  "num_heads": 4,
  "lookback_length": 4096
}
```

**数据情况**:
- 验证集: 1260 样本
- 训练集: 未知（可能也不大）
- 信号强度: R²=2-4%（很弱）

**问题**:
1. **模型太复杂**: 3 层 Transformer + 4096 长序列 → 大量参数
2. **数据太少**: ~1000-5000 样本 → 不足以训练深度模型
3. **信号太弱**: R²=2-4% → 容易过拟合到噪声

**结果**:
- 模型在训练集上过拟合到噪声
- 在验证集上退化到预测均值（无条件分位数）
- Loss 看起来很好，但没有学到条件关系

---

## 💡 修复方案

### 方案 A: 简化模型（推荐）⭐

**修改配置**:
```json
{
  "d_model": 64,           // 从 128 → 64
  "num_layers": 2,         // 从 3 → 2
  "num_heads": 2,          // 从 4 → 2
  "lookback_length": 1024, // 从 4096 → 1024
  "num_features": 7        // 从 8 → 7 (移除常数特征)
}
```

**原因**:
- 更小的模型更容易学到弱信号
- 更短的序列减少过拟合
- 更少的参数需要更少的数据

**预期结果**:
```
h=1:  corr=0.15-0.20, Scale ratio=0.9-1.1
h=5:  corr=0.18-0.23, Scale ratio=0.9-1.1
h=20: corr=0.12-0.18, Scale ratio=0.9-1.1
h=60: corr=0.15-0.22, Scale ratio=0.9-1.1
```

### 方案 B: 增加正则化

```python
# 1. Weight decay
optimizer = optax.adamw(learning_rate=1e-4, weight_decay=1e-4)

# 2. 更小的学习率
learning_rate = 5e-5

# 3. Dropout (如果支持)
dropout_rate = 0.1
```

### 方案 C: 使用线性基线

如果深度模型持续失败，使用简单模型：

```python
# 线性分位数回归
# 应该能达到 corr=0.15-0.21
# 如果深度模型不能超过它，说明架构有问题
```

---

## 📋 实施步骤

### Step 1: 重新训练

```bash
cd /home/v/Documents/work/1_open_source/alphagenome_research

# 使用简化配置
python src/alphagenome_research/alphatrade/train_loop_minimal.py \
  --config src/alphagenome_research/alphatrade/config_simple.json \
  --workdir src/alphagenome_research/alphatrade/runs/jm_v03_simple
```

### Step 2: 监控训练

每 500 steps 检查：

```python
# 1. eval_loss 是否持续下降
# 2. 预测的 std 是否接近真实值的 std
# 3. 预测与真实值的相关性是否 > 0.1

# 如果训练 5000 steps 后 corr < 0.1，停止
# 说明模型架构有问题
```

### Step 3: 验证校准

```bash
# 修改诊断脚本中的路径
# 然后运行
python src/alphagenome_research/alphatrade/diagnose_calibration.py

# 期望：
# - corr > 0.15
# - Scale ratio 在 0.8-1.2
# - Coverage 偏差 < 0.1
```

---

## 📊 诊断工具

### 1. 数据信号检查

```bash
python src/alphagenome_research/alphatrade/check_data_signal.py
```

**输出**:
- 每个特征与目标的相关性
- 线性模型的 R² 和相关性
- 判断数据是否有足够信号

### 2. 校准诊断

```bash
python src/alphagenome_research/alphatrade/diagnose_calibration.py
```

**输出**:
- Quantile crossing rate（应该 ≈ 0）
- Bias 和 Scale（应该接近 0 和 1）
- Coverage vs Expected（应该接近）
- Horizon 对齐检查（相关性矩阵）

---

## 🎓 技术洞察

### 为什么深度模型会失败？

**过拟合到噪声的典型模式**:

1. **训练早期**: 模型学到一些信号，loss 下降
2. **训练中期**: 模型开始记忆训练集的噪声
3. **训练后期**: 模型在训练集上表现很好，但泛化性能差
4. **验证时**: 模型退化到预测均值（最安全的策略）

**为什么预测几乎是常数？**

```python
# 当模型不确定时，最优策略是预测边缘分布：
# - 最小化 Pinball Loss
# - 避免大的预测错误
# - 但没有条件信息

# 这就是你的模型现在的状态
```

### 金融预测的现实

- R²=2-4% 在金融中已经很好了
- 大多数金融预测模型的 R² < 10%
- 如果能稳定达到 R²=5%，已经可以盈利

**所以**:
- 不要期望 R²=50%
- 关注相对改进（深度模型 vs 线性基线）
- 如果深度模型不能超过线性基线，说明架构有问题

---

## ✅ 总结

### 问题确认

1. ✅ 数据有信号（R²=2-4%）
2. ✅ 模型没学到信号（corr≈0）
3. ✅ 校准失败（coverage 偏差大）
4. ✅ 根本原因：模型太复杂，数据太少

### 修复方案

1. ⭐ **推荐**: 使用简化配置重新训练
2. 🔧 **备选**: 增加正则化
3. 🎯 **基线**: 使用线性模型作为对比

### 预期结果

- 简化模型应该能达到 corr=0.15-0.20
- 接近或超过线性基线
- Coverage 偏差 < 0.1
- Scale ratio 在 0.8-1.2

### 下一步

```bash
# 1. 使用 config_simple.json 重新训练
# 2. 每 500 steps 检查 corr 是否 > 0.1
# 3. 训练完成后运行 diagnose_calibration.py
# 4. 如果仍然失败，使用线性基线
```

---

**文档**:
- `CALIBRATION_FIX_GUIDE.md` - 详细修复指南
- `diagnose_calibration.py` - 校准诊断工具
- `check_data_signal.py` - 数据信号检查工具
- `config_simple.json` - 简化配置

**状态**: ✅ 诊断完成，等待用户重新训练
